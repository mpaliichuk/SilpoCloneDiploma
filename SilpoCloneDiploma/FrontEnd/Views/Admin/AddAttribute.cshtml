@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@* <div class="content-wrapper">
    <div class="form-container">
        <h2 style="color:#004d00;">Редагування атрибуту</h2>
        <form asp-action="EditAttribute" asp-controller="Admin" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Назва</label>
                <input type="text" class="form-control" id="name" name="name" required />
                <span class="text-danger" asp-validation-for="title"></span>
            </div>
            <div class="form-group">
                <label for="index">Індекс</label>
                <input type="text" class="form-control" id="index" name="index" required />
                <span class="text-danger" asp-validation-for="index"></span>
            </div>
            <div class="form-group">
                <label for="group">Група</label>
                <input type="text" class="form-control" id="group" name="group" required />
                <span class="text-danger" asp-validation-for="group"></span>
            </div>
            <div class="form-group">
                <input type="file" id="photos" name="photos" class="form-control-file" style="display: none;" multiple />
                <div id="photo-previews" style="display: flex; flex-wrap: wrap; gap: 10px; margin-top: 20px;">
                    <!-- Динамічні прев'ю з'являться тут -->
                </div>
                <button type="button" class="btn btn-primary myBtnForPhoto" onclick="document.getElementById('photos').click();">Завантажити файли</button>
                <span id="file-names" style="margin-left: 10px;">Шлях до файлів</span>
            </div>
            <button type="submit" class="btn btn-primary">Зберегти</button>
            <button type="reset" class="btn btn-secondary" onclick="resetForm()">Відмінити</button>
        </form>
    </div>
</div>

<script>
    document.getElementById("photos").addEventListener("change", function() {
        const previewsContainer = document.getElementById("photo-previews");
        const fileNames = document.getElementById("file-names");

        // Очищаємо попередній попередній перегляд
        previewsContainer.innerHTML = "";

        const files = this.files;
        let fileNamesText = '';

        for (let i = 0; i < files.length; i++) {
            const file = files[i];

            // Додаємо ім'я файлу до списку
            fileNamesText += file.name + (i < files.length - 1 ? ', ' : '');

            // Створюємо елемент для попереднього перегляду
            const imgPreview = document.createElement("img");
            imgPreview.style.maxWidth = "200px";
            imgPreview.style.marginRight = "10px";

            const reader = new FileReader();
            reader.onload = function(e) {
                imgPreview.src = e.target.result;
            };
            reader.readAsDataURL(file);

            previewsContainer.appendChild(imgPreview);
        }

        // Відображаємо імена файлів
        fileNames.textContent = fileNamesText || "Шлях до файлів";
    });
</script> *@
